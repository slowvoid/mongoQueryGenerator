db.Store.aggregate([
    {"$match": {
        $expr: {
            $eq: ["$_id", "%DB_KEY%"]
        }
    },
    {$lookup: {
        from: 'Product',
        foreignField: 'StoreID',
        localField: '_id',
        as: 'data_Product'
    }}
], {allowDiskUse: true})