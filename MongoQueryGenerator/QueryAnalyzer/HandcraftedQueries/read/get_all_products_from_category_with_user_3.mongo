db.Category.aggregate([
    {"$match": {
        $expr: {
            $eq: ["$_id", "%DB_KEY%"]
        }
    },
    {"$lookup": {
        from: 'Product',
        foreignField: 'CategoryID',
        localField: '_id',
        as: 'data_Product',
    }}
], {allowDiskUse: true})